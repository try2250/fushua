<!--pages/teacher/classes/classes.wxml-->
<view class="classes-container academy-teacher">
  <view class="teacher-ledger-header landscape-wash">
    <view class="teacher-ledger-copy">
      <text class="hero-eyebrow">教师管理旅程</text>
      <text class="hero-title">班级台账</text>
      <text class="hero-desc">教学管理、测评跟踪与学生成长记录，从班级开始。</text>
    </view>
    <button class="btn-create academy-primary" bindtap="handleCreateClass">
      <text>创建班级</text>
    </button>
  </view>

  <!-- 头部操作栏 -->
  <view class="header-bar academy-summary">
    <view class="header-info">
      <text class="info-text">共 {{classes.length}} 个班级</text>
    </view>
    <text class="seal-mini">台账</text>
  </view>

  <!-- 班级列表 -->
  <view class="classes-list">
    <view
      class="class-card ledger-card"
      wx:for="{{classes}}"
      wx:key="id"
      bindtap="handleViewClass"
      data-id="{{item.id}}"
    >
      <!-- 班级图标 -->
      <view class="class-icon">
        <text class="icon-text">{{item.iconText}}</text>
      </view>

      <!-- 班级信息 -->
      <view class="class-info">
        <view class="class-name">{{item.name}}</view>
        <view class="class-meta">
          <text class="meta-item">👥 {{item.member_count}} 人</text>
          <text class="meta-divider">|</text>
          <text class="meta-item">📅 {{item.created_at}}</text>
        </view>
      </view>

      <!-- 操作按钮 -->
      <view class="class-actions" catchtap="stopPropagation">
        <button
          class="btn-action btn-edit"
          catchtap="handleEditClass"
          data-id="{{item.id}}"
          data-name="{{item.name}}"
        >
          编辑
        </button>
        <button
          class="btn-action btn-delete"
          catchtap="handleDeleteClass"
          data-id="{{item.id}}"
          data-name="{{item.name}}"
        >
          删除
        </button>
      </view>
    </view>
  </view>

  <!-- 空状态 -->
  <view class="empty-container" wx:if="{{!loading && classes.length === 0}}">
    <text class="empty-icon">📚</text>
    <text class="empty-text">暂无班级</text>
    <text class="empty-desc">点击右上角创建第一个班级</text>
  </view>

  <!-- 加载状态 -->
  <view class="loading-container" wx:if="{{loading}}">
    <text>加载中...</text>
  </view>
</view>
