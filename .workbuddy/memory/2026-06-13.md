# 2026-06-13 工作日志

## Plan 1.1 多租户基础设施 + Question 资源示范 — 完成

**状态**: ✅ 12/12 tasks 完成，11 个 commit

### 完成内容
- `app/core/tenant.py` — TenantContext dataclass + get_tenant_context 依赖 + tenant_filter 查询辅助
- `app/services/question_service.py` — 3 个 tenant-aware 方法（get_questions_for_tenant, get_question_by_id_for_tenant, get_random_questions_for_tenant）
- `app/api/v1/questions.py` — 全部 6 个端点接入 get_tenant_context（list/detail/create/update/delete/random）
- `tests/test_tenant_isolation.py` — 15 个隔离测试
- `tests/test_route_audit.py` — API 路由 tenant 依赖审计扫描器
- `tests/fixtures_tenant.py` — 多租户 fixture（teacher_a/b, token, class_b_with_student）
- `tests/test_questions_api.py` — 适配 student create 测试（补 class_id）

### 验收
- compileall: ✅
- test_tenant_isolation.py: 15/15 passed ✅
- test_route_audit.py: 1 xfailed ✅
- 全量测试: 573 passed, 1 已有 miniprogram compat fail

### 架构决策
- TenantContext.tenant_id = 老师 user.id
- Teacher: Bearer Token 直接解析 user.id
- Student: 通过 class_id 查询参数 → ClassMember → ClassGroup.created_by
- tenant_filter: 对 SQLAlchemy Query 按 created_by 过滤
- 白名单: /api/v1/auth/*, /health, /api/docs 等不需要 tenant

### 下一步
Plan 1.2A — Class / Assignment 接入 tenant

---

## Plan 1.2A 多租户扩展到 Class & Assignment — 完成

**状态**: ✅ 10/10 tasks 完成

### 完成内容
- `app/services/class_service.py` — +get_classes_for_tenant, get_class_by_id_for_tenant
- `app/services/assignment_service.py` — +get_assignments_for_tenant, get_assignment_by_id_for_tenant
- `app/api/v1/classes.py` — 7 个端点接入 TenantContext（/join 保留 get_current_user）
- `app/api/v1/assignments.py` — 8 个端点接入 TenantContext
- `tests/test_tenant_isolation_class.py` — 9 个隔离测试
- `tests/test_tenant_isolation_assignment.py` — 9 个隔离测试
- `tests/test_route_audit.py` — COVERED 扩到 questions+classes+assignments

### 验收
- compileall: ✅
- test_tenant_isolation_class.py: 9/9 ✅
- test_tenant_isolation_assignment.py: 9/9 ✅
- test_route_audit.py: 1 xfailed ✅
- 全量测试: 592 passed, 1 xfailed（0 新 fail）

### 跨租户入口
`/api/v1/classes/{class_id}/join` — 学生主动加入，audit 精确白名单豁免

### 下一步
Plan 1.2B — Records / Users / Announcements / Classroom 接入 tenant
